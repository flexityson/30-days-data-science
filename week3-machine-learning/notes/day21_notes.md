# What Streamlit is in my own words
so streamlit is library that you can use this to host web and can ask for user input without need to code throught html css or java
# the difference between a script and a web app 
for clients because script only done one thing which is prediction, you can't decide which one the way you want it to. It just show you the accuracy. and a web app is allow client to input the value or anything they want to know exactly what is turn out to be liked that. "A client doesn't need Python installed to use a web app — they just open a browser.
# Streamlit widgets I used today — 
st.slider, use can slide between number or value
st.selectbox, user can select the thing they want in categories
st.button, user can press on
st.success, show green message (good outcome)
st.error, show red message (bad outcome)
# Bugs I fixed today and what caused them
1. the X which is target in thier columns need to exactly match the input columns input when we want to ask user for inpuit 
2. Forgot to close square bracket in input for user
# Self-review questions-at least 3
1. Why do we have to use streamlit?
2. When do we use a web app or when to use a script?
3. How can I use this kind a web app to test my model accuracy?