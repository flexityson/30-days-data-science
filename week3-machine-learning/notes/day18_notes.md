Self-note for D18

# What is Random Forest in my own words?

So random forest is just stack up more of tree which is decision tree to make it has more predicted results, so that it is closed to accuracy results. just like you take 100 feedback from 100 user for the one things and you can make final decision base on those feedback, not as 1 feedback from 1 user, you can't assume the final results yet.

# What is ensemble learning

bascially it is concept that combine multiple models to get better results rather than just using one model alone.

# Feature importance — what it is and what the scores mean

Feature importance is feature that tell which one is matter the most. and the score mean the percentage that the decision making that feature contribute to.

# Comparison table — Logistic Regression vs Decision Tree vs Random Forest accuracy
# model

- Logistic Regression 0.79
- Decision Tree 0.77
- Random Forest 0.78
# Why Random Forest reduces overfitting

because you use a lots of decision maker to predict one results, so it gets closed to the results that it want to get.

# Self-review questions — at least 3

When to use all of these 3 components?
Why it is matter to use Random Forest?
Is there anyway more we can analysis what is the most important in our data?